@using OrderingSystem.Domain
@{
    ViewBag.Title = "规格列表";
}
@model OrderingSystem.Admin.Models.DishesSpecVM
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-sm-12">
            <div class="ibox float-e-margins">
                <div class="ibox-content">
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th class="text-center">序号</th>
                                    <th class="text-center">规格名称</th> 
                                    @*<th class="text-center">创建时间</th>*@
                                    <th>操作</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.DishesSpecList != null && Model.DishesSpecList.Count > 0)
                                {
                                    foreach (var item in Model.DishesSpecList)
                                    {
                                        <tr>
                                            <td class="text-center">@item.DishesSpecId</td>
                                            <td class="text-center">@item.Name</td> 
                                            @*<td class="text-center">@item.DishesLable.CreateTime</td>*@
                                            <td>
                                                <a href="/DishesSpec/Edit?Id=@item.DishesSpecId&DishesId=@item.DishesId" class="btn btn-info"><i class="fa fa-paste padding-right-4"></i>&nbsp;编辑名称</a>
                                                <a href="/DishesSpec/DetailList?Id=@item.DishesSpecId&DishesId=@item.DishesId" class="btn btn-info"><i class="fa fa-paste padding-right-4"></i>&nbsp;编辑子项</a>
                                                <button onclick="del('/DishesSpec/Delete','/DishesSpec/List/@item.DishesId','@item.DishesSpecId')" class="btn btn-danger"><i class="fa fa-times padding-right-4"></i>&nbsp;删除</button>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="btn-group">
                                <a href="/DishesSpec/Edit?DishesId=@Model.Id" class="btn btn-info" data-refresh="0"><i class="fa fa-plus padding-right-4"></i>&nbsp;添加</a>
                                <a href="/DishesSpec/List/@Model.Id" class="btn btn-info" style="margin-left:10px;" data-refresh="0"><i class="fa fa-refresh padding-right-4"></i>&nbsp;刷新</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
