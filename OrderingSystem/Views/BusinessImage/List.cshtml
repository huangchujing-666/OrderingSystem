@{
    ViewBag.Title = "商家图片列表";
}
@model OrderingSystem.Admin.Models.BusinessImageVM
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-sm-12">
            <div class="ibox float-e-margins">
                <div class="ibox-content">
                    
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th class="text-center">序号</th>
                                    <th class="text-center">商家名称</th>
                                    <th class="text-center">图片</th>
                                    <th class="text-center">图片类型</th>
                                    <th class="text-center">排序号</th> 
                                    <th class="text-center">状态</th>
                                    <th class="text-center">创建时间</th> 
                                    <th>操作</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Paging.Items != null && Model.Paging.Items.Count > 0)
                                {
                                    foreach (var item in Model.Paging.Items)
                                    {
                                        <tr>
                                            <td class="text-center">@item.BusinessImageId</td>
                                            <td class="text-center">@item.BusinessInfo.Name</td> 
                                            <td class="text-center">@item.BaseImageId</td>
                                            <td class="text-center">
                                                @if (item.Type == 1)
                                                {
                                                    @("展示图")
                                                }
                                                @if (item.Type == 2)
                                                {
                                                    @("全景图")
                                                }
                                                @if (item.Type == 3)
                                                {
                                                    @("实景图")
                                                }
                                            </td> 
                                            <td class="text-center">@item.SortNo</td>
                                            <td class="text-center">
                                                @if (item.Status == 0)
                                                {
                                                    <button type="button" class="btn btn-danger btn-xs">无效</button>
                                                }
                                                @if (item.Status == 1)
                                                {
                                                    <button type="button" class="btn btn-info btn-xs">有效</button>
                                                }
                                            </td>
                                            <td class="text-center">@item.CreateTime</td>
                                            <td> 
                                                <a href="/BusinessImage/Edit?id=@item.BusinessImageId&v=0" class="btn btn-info"><i class="fa fa-paste padding-right-4"></i>&nbsp;编辑</a>
                                                @if (item.Status == 0)
                                                {
                                                    <button onclick="enable('/BusinessImage/UpdateStatus','/BusinessImage/List','@item.BusinessImageId','@((int)OrderingSystem.Domain.EnumHelp.EnabledEnum.有效)')" class="btn btn-primary"><i class="fa fa-check padding-right-4"></i>&nbsp;启用</button>
                                                }
                                                @if (item.Status == 1)
                                                {
                                                    <button onclick="disable('/BusinessImage/UpdateStatus','/BusinessImage/List','@item.BusinessImageId','@((int)OrderingSystem.Domain.EnumHelp.EnabledEnum.无效)')" class="btn btn-warning"><i class="fa fa-warning padding-right-4"></i>&nbsp;禁用</button>
                                                }
                                                <button onclick="del('/BusinessImage/Delete','/BusinessImage/List','@item.BusinessImageId')" class="btn btn-danger"><i class="fa fa-times padding-right-4"></i>&nbsp;删除</button>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="btn-group">
                                <a href="/BusinessImage/Edit?BusinessId=@Model.Id" class="btn btn-info" data-refresh="0"><i class="fa fa-plus padding-right-4"></i>&nbsp;添加</a>
                                <a href="/BusinessImage/List?Id=@Model.Id" class="btn btn-info" style="margin-left:10px;" data-refresh="0"><i class="fa fa-refresh padding-right-4"></i>&nbsp;刷新</a>
                            </div>
                            @Html.Raw(Model.Paging.Text)
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
