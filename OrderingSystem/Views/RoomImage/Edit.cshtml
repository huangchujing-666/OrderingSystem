@{
    ViewBag.Title = "房间展示图编辑";
}
@model OrderingSystem.Admin.Models.RoomImageVM
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-sm-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>
                        房间展示图编辑 <small>添加、修改</small>
                    </h5>
                </div>
                <div class="ibox-content">
                    <form class="form-horizontal">
                        <input type="hidden" name="id" id="id" value="@Model.RoomImage.RoomImageId">
                        <input type="hidden" name="RoomId" id="RoomId" value="@Model.RoomImage.RoomId">
                        <input type="hidden" id="imgInfoId" value="@Model.RoomImage.BaseImageId" />
                        <input type="hidden" id="oldImgInfoId" value="@Model.RoomImage.BaseImageId" />
                         
                        <div class="form-group">
                            <label class="col-sm-2 control-label">商家Logo图</label>
                            <div class="col-sm-6">
                                <input id="filename" name="filename" type="file" class="file-loading" multiple data-show-upload="true" data-show-caption="true">
                            </div>
                        </div>
                        <div class="hr-line-dashed"></div>
                        <div class="form-group">
                            <label class="col-sm-2 control-label">图片类型</label>
                            <div class="col-sm-4">
                                <select class="form-control inline" style="width:150px;" name="type" id="type">
                                    <option value="@((int)OrderingSystem.Domain.EnumHelp.BusinessImageTypeEnum.展示图)">展示图</option>
                                    @*<option @(Model.RoomImage.Type == (int)OrderingSystem.Domain.EnumHelp.BusinessImageTypeEnum.全景图 ? "selected" : "") value="@((int)OrderingSystem.Domain.EnumHelp.BusinessImageTypeEnum.全景图)">全景图</option>*@ 
                                </select>
                            </div>
                        </div>
                        <div class="hr-line-dashed"></div>
                        @*<div class="form-group">
                            <label class="col-sm-2 control-label">排序号</label>
                            <div class="col-sm-4">
                                <input type="number" class="form-control" placeholder="请输入排序号" maxlength="4" name="sortNo" id="sortNo" value="@Model.BusinessImage.SortNo">
                            </div>
                        </div>
                        <div class="hr-line-dashed"></div>*@
                        <div class="form-group">
                            <div class="col-sm-4 col-sm-offset-2">
                                <button id="btnSave" class="btn btn-primary" type="button">保存</button>
                                <a class="btn btn-white" href="javascript:history.go(-1);">取消</a>
                            </div>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>
@section script{
    <script src="@("/Scripts/plugins/fileinput/fileinput.js"+OrderingSystem.Admin.Versions.Ver)"></script>
    <script src="@("/Scripts/plugins/fileinput/zh.js"+OrderingSystem.Admin.Versions.Ver)"></script>
    <script src="@("/Scripts/ViewJs/RoomImage/RoomImage.js"+OrderingSystem.Admin.Versions.Ver)"></script>
    <script type="text/javascript">


        //商家缩略图
        var img = [];
        if ('@Model.ImgInfo.Path' != '') {
            var str = '<img src="@(Model.ImgInfo.Source + Model.ImgInfo.Path)" class="kv-preview-thumb kv-file-content file-preview-image" />';
            img.push(str);
        }
        //缩略图上传js
        $('#filename').fileinput({
            language: 'zh',
            allowedFileTypes: ['image'],
            allowedFileExtensions: ['jpg', 'png', 'gif'],
            uploadUrl: '/Uploader/ImgFile?folder=RoomImage&title=房间展示图片',
            showUpload: false,  //是否显示上传按钮
            showRemove: true,   //显示移除按钮 
            showCaption: true,  //是否显示标题
            maxFileCount: 1,    //表示允许同时上传的最大文件个数
            maxFileSize: 0,   //单位为kb，如果为0表示不限制文件大小
            initialPreview: img,
            dropZoneTitle: '请选择需要上传的图片(注意文件不宜太大)',
        });
        //上传成功回调事件
        $("#filename").on("fileuploaded", function (event, data, previewId, index) {
            $('#imgInfoId').val(data.response);
        });
        //移除事件
        $("#filename").on("fileclear", function (event, data, previewId, index) {
            $.post("/Uploader/DeleteFile", { Id: $('#imgInfoId').val() }, function (data) { });
        });
    </script>
}

