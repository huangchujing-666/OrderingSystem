@{
    ViewBag.Title = "房间列表";
}
@model OrderingSystem.Admin.Models.RoomVM
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-sm-12">
            <div class="ibox float-e-margins">
                <div class="ibox-content">
                    <div class="row" style="padding:10px 10px;">
                        <form action="/Room/List" method="get">
                            <div class="col-sm-3">
                                房间名称：
                                <input type="text" name="queryName" class="form-control inline" style="width:70%;" placeholder="请输入查询名称" value="@(!string.IsNullOrWhiteSpace(Model.QueryName)?Model.QueryName:"")">
                            </div>
                            <div class="col-sm-3">
                                商家：
                                <input type="text" name="queryBusinessmanName" class="form-control inline" style="width:70%;" placeholder="请输入查询商家" value="@(!string.IsNullOrWhiteSpace(Model.QueryBusinessmanName)?Model.QueryBusinessmanName:"")">
                            </div>
                            <div class="col-sm-3">
                                <button type="submit" class="btn btn-info">搜索</button>
                            </div>
                        </form>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th class="text-center">序号</th> 
                                    <th class="text-center" style="width:300px;">产品名称</th>
                                    <th class="text-right">原价(元)</th>
                                    <th class="text-right">优惠(元)</th>
                                    <th class="text-center">商家</th> 
                                    <th class="text-center">状态</th>
                                    <th>操作</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Paging.Items != null && Model.Paging.Items.Count > 0)
                                {
                                    foreach (var item in Model.Paging.Items)
                                    {
                                        <tr>
                                            <td class="text-center">@item.RoomId</td> 
                                            <td class="text-center">@item.Name</td>
                                            <td class="text-right">@item.OrignPrice</td>
                                            <td class="text-right">@item.RealPrice</td>
                                            <td class="text-center">@item.BusinessInfo.Name</td> 
                                            <td class="text-center">
                                                @if (item.Status == (int)OrderingSystem.Domain.EnumHelp.EnabledEnum.无效)
                                                {
                                                    <span class="label label-danger">无效</span>
                                                }
                                                @if (item.Status == (int)OrderingSystem.Domain.EnumHelp.EnabledEnum.有效)
                                                {
                                                    <span class="label label-info">有效</span>
                                                }
                                            </td>
                                            <td>
                                                <button onclick="iframe('标签编辑','/RoomLabel/List/@item.RoomId')" class="btn btn-success">标签</button> 
                                                <button onclick="iframe('展示图','/RoomImage/List/@item.RoomId')" class="btn btn-success">展示图</button>
                                                <a href="/Room/Edit/@item.RoomId" class="btn btn-info"><i class="fa fa-paste padding-right-4"></i>&nbsp;编辑</a>
                                                @if (item.Status == 0)
                                                {
                                                    <button onclick="enable('/Room/UpdateStatus','/Room/List?RefreshFlag=1','@item.RoomId','@((int)OrderingSystem.Domain.EnumHelp.EnabledEnum.有效)')" class="btn btn-primary"><i class="fa fa-check padding-right-4"></i>&nbsp;启用</button>
                                                }
                                                @if (item.Status == 1)
                                                {
                                                    <button onclick="disable('/Room/UpdateStatus','/Room/List?RefreshFlag=1','@item.RoomId','@((int)OrderingSystem.Domain.EnumHelp.EnabledEnum.无效)')" class="btn btn-warning"><i class="fa fa-warning padding-right-4"></i>&nbsp;禁用</button>
                                                }

                                                <button onclick="del('/Room/Delete','/Room/List','@item.RoomId')" class="btn btn-danger"><i class="fa fa-times padding-right-4"></i>&nbsp;删除</button>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="btn-group">
                                <a href="/Room/Edit" class="btn btn-info" data-refresh="0"><i class="fa fa-plus padding-right-4"></i>&nbsp;添加</a>
                                <a href="/Room/List" class="btn btn-info" style="margin-left:10px;" data-refresh="0"><i class="fa fa-refresh padding-right-4"></i>&nbsp;刷新</a>
                            </div>
                            @Html.Raw(Model.Paging.Text)
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section script{
    <script type="text/javascript">
        $(document).ready(function () {
            $('.fancy').fancybox();
        });
    </script>
}